apiVersion: 1

datasources:
- name: PostgreSQL
  type: postgres
  access: proxy
  url: ${POSTGRE_HOST}:5432
  database: ${DATABASE_NAME}
  user: ${DATABASE_USER}
  secureJsonData:
    password: ${DATABASE_PASSWORD}
  jsonData:
    sslmode: disable
